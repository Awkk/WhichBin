function nextCard(index) {
    "use strict";
    imageCard[index]();
    window.setTimeout(
        nextCard.bind(undefined, (index + 1) % nextCard.length),
        1000
    );
}

this is the function i found online for the looping... but i can't figure out a syntax problem to run it